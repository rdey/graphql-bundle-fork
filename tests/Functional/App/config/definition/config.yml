imports:
    - { resource: ../config.yml }

redeye_graphql:
    definitions:
        schema:
            query: RootQuery
        mappings:
            types:
                -
                    type: yaml
                    dir: "%kernel.project_dir%/config/definition/mapping"
